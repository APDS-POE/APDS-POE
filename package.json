{
    "name": "frontend-backend-runner",
    "version": "1.0.0",
    "scripts": {
        "frontend": "cd frontend && npm start",
        "backend": "cd backend && npm start",
        "start": "concurrently \"npm run frontend\" \"npm run backend\""
    },
    "devDependencies": {
        "concurrently": "^7.6.0",
        "cross-env": "^7.0.3"
    },
    "dependencies": {
        "express-brute": "^1.0.1"
    }
}
